# --- Arquivo de Configuração de Ambiente para Vagrant (.env) ---
# Descomente e modifique as linhas conforme necessário.
# Se uma variável não estiver definida aqui, o Vagrantfile usará o valor padrão.

# --- Configuração do ngrok ---
# Cole seu authtoken do ngrok aqui para configuração automática na VM.
# Você pode encontrá-lo em: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN="2yCeckficrI6In4RFBbAeLBreS7_6jsMs67c45N7UKEzt6GVV"


# --- Configurações da Box Vagrant ---
# Box a ser usada (ex: "ubuntu/jammy64", "generic/ubuntu2204")
VM_BOX="bento/centos-stream-10"

# Versão específica da box para garantir consistência
VM_BOX_VERSION="202502.21.0"

# --- Configurações da VM ---
# Nome do host para a VM
VM_HOSTNAME="DeusExMachina-VM"

# Endereço IP para a rede privada da VM
VM_IP_PRIVATE="192.168.56.10"

# Memória alocada para a VM em Megabytes (ex: 2048 para 2GB, 4096 para 4GB)
VM_MEMORY="8196"  # 8GB

# Número de CPUs alocadas para a VM
VM_CPUS="8"

# Tamanho do disco para a VM (requer o plugin vagrant-disksize)
# Ex: "50GB", "100GB"
VM_DISKSIZE="60GB"

# --- Configuração do Domínio de Desenvolvimento ---
# Domínio local a ser usado pela VM (ex: .test, .local, .dev)
# Este domínio será resolvido para o IP da VM.
VM_DEV_DOMAIN="deusex.io"

# --- Configurações de Interface do Vagrantfile ---
# Define se emojis serão usados nos logs do Vagrantfile (true/false)
USE_EMOJIS="true"

# --- Outras Configurações Potenciais (Exemplos) ---
# Você pode adicionar outras variáveis que seus scripts ou Vagrantfile possam usar.
# Por exemplo, se você quiser passar uma porta específica para um script:
# MINHA_PORTA_CUSTOMIZADA="8888"
